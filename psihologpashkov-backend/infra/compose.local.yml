services:
  postgres:
    image: postgres:16-alpine
    container_name: psihologpashkov-postgres-local
    restart: unless-stopped
    environment:
      POSTGRES_DB: psihologpashkov
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - psihologpashkov_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d psihologpashkov"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  psihologpashkov_postgres_data: